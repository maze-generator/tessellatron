// dependency imports
import chai from 'chai'
const { expect } = chai

// test the project
describe('Smoke Test', () => {
	it('should compute basic addition', () => {
		expect(2 + 2).to.equal(4)
	})
})
